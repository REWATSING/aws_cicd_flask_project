version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.8
  pre_build:
    commands:
      - pip install -r requirements.txt
  build:
    commands:
      - aws cloudformation package --template-file sam.yaml --s3-bucket mybucket --output-template-file output-sam.yaml
artifacts:
  type: zip
  files:
    - output-sam.yaml
